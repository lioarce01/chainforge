apiVersion: v1
kind: Service
metadata:
  name: {{ include "chainforge.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chainforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "chainforge.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
